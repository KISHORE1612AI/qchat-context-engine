# --- core server ---
fastapi==0.115.0
uvicorn[standard]==0.30.6
websockets==12.0
python-multipart==0.0.6
requests==2.32.3
rich==13.8.1

# --- chroma + numpy + pydantic pins (compatibility) ---
chromadb==0.4.22
numpy==1.26.4
pydantic==1.10.13

# --- build tools (exact so builds are reproducible) ---
setuptools==70.3.0
wheel==0.43.0
